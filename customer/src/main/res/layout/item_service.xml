<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardBackgroundColor="@color/bgWhite"
    app:cardMaxElevation="20dp"
    app:cardCornerRadius="@dimen/card_radius"
    app:cardElevation="@dimen/card_elevation"
    android:layout_marginHorizontal="@dimen/card_margin"
    android:layout_marginVertical="@dimen/margin_item"
    android:forceHasOverlappingRendering="true"
    android:overScrollMode="always"
    android:foreground="?selectableItemBackground"
    android:clickable="true"
    android:layout_height="wrap_content">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_item"
        android:orientation="horizontal">
        <ImageView
            android:layout_width="@dimen/item_service_size"
            android:layout_height="@dimen/item_service_size"
            android:src="@drawable/ic_app"
            android:id="@+id/item_service_icon"
            android:scaleType="centerCrop"
            />
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:orientation="vertical">
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/title_service"
                android:layout_marginLeft="@dimen/margin_item"
                android:id="@+id/item_service_txt_title"
                android:textColor="@color/textColor"
                android:fontFamily="sans-serif-black"
                android:textSize="@dimen/font_size_title_item"/>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginLeft="@dimen/margin_item"
                android:textColor="@color/textColor"
                android:id="@+id/item_service_txt_sub_title"
                android:text="@string/service_sub_title"
                android:fontFamily="sans-serif-smallcaps"
                android:textSize="@dimen/font_size_sub_title"/>
        </LinearLayout>


    </LinearLayout>

</android.support.v7.widget.CardView>